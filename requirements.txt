# Core PDF processing
PyMuPDF>=1.24.0
pyyaml>=6.0

# Vision model — MiniCPM-V-2.6
Pillow>=10.0.0
transformers>=4.40.0
accelerate>=0.30.0
sentencepiece>=0.1.99

# PyTorch — install separately with CUDA support (see README)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126

# Flash Attention (Linux only, strongly recommended for performance)
# pip install flash-attn --no-build-isolation

# Optional: Docling parser
# pip install docling>=2.0.0
